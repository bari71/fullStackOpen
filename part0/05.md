```mermaid
    sequenceDiagram
        participant browser
        participant server

        browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/data.json
        activate server
        server-->>browser: data.json
        deactivate server

        Note left of server: The browser handles the code to display the notes using the DOM-API, but relies on the server's data on the notes. The server returns the data.json file which is used by the browser to construct the webpage on the browser side.