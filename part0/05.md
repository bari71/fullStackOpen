```mermaid
    sequenceDiagram
        participant browser
        participant server

        browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
        activate server
        server-->>browser: 201 Created
        deactivate server

        Note left of server: The browser sends a POST request to the server and updates the webpage using the DOM-API. Full reload of the page is based on the updated database which preserves the newly added notes.

